# 高并发与风控自愈架构遗留记录及后续建议 (TODO)

## 待完成及运营阶段观察事项：
1. **风控锁向前端状态同步**
   - 当前的 `suspendUntil` 仅在内存里维持对当次挂机循环的锁死。如果在停止挂机前有 UI 想让用户知晓，可以在未来将悬停状态打入 Redis 并在面板通过 Websocket 广播展示账号带有一个“☕️风控休息中”的特殊状态标识，避免用户对挂机不动产生误解。

2. **验证休眠时长的精准度**
   - 目前写死的风控锁为 30 分钟 (1800000ms)，若 Tencent 的惩罚随次数成阶梯上涨，后续可将其配置至数据库。如果休眠后仍遇 1002003，可以动态累加到 2小时、24小时。

## 怎么处理 TODO 
当前所有关于稳定性的阻断点已经解除。后续如有前端状态展现需求，可在下一个 Milestone 中实施。若现在已满足基本需求，可以直接起服验证挂机了！
